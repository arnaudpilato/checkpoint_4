{% extends 'admin.html.twig' %}

{% block title %}Edit Brand{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center my-5">Modifier</h1>
            {{ include('admin/brand/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        <div class="text-right">
            <a class="btn btn-secondary" href="{{ path('admin_brand_index') }}">Retour</a>
        </div>
    </div>
{% endblock %}
