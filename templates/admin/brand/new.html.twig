{% extends 'admin.html.twig' %}

{% block title %}Nouvelle marque{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center my-5">Ajouter une marque</h1>
        {{ include('admin/brand/_form.html.twig') }}
        <div class="text-right">
            <a class="btn btn-secondary mt-5" href="{{ path('admin_brand_index') }}">Retour</a>
        </div>
    </div>
{% endblock %}
