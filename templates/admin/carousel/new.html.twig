{% extends 'admin.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% block title %}Nouvelle image{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center my-5">Nouvelle image</h1>
        {{ include('admin/carousel/_form.html.twig') }}
        <div class="text-right mt-5">
            <a class="btn btn-secondary" href="{{ path('admin_carousel_index') }}">Retour</a>
        </div>
    </div>
{% endblock %}
