<form method="post" action="{{ path('admin_discipline_delete', {'id': discipline.id}) }}" onsubmit="return confirm('Êtes-vous sur de vouloir supprimer cette image ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ discipline.id) }}">
    <button class="btn btn-secondary mt-2">Delete</button>
</form>
