{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="text-center">
        <button class="btn btn-secondary mt-5">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
