{% extends 'base.html.twig' %}

{% block title %}Equipment{% endblock %}

{% block body %}
    <h1>Equipement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipment.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ equipment.name }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ equipment.picture }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ equipment.description }}</td>
            </tr>
            <tr>
                <th>Brand</th>
                <td>{{ equipment.brand }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_equipment_index') }}">back to list</a>

    <a href="{{ path('admin_equipment_edit', {'id': equipment.id}) }}">edit</a>

    {{ include('admin/equipment/_delete_form.html.twig') }}
{% endblock %}
