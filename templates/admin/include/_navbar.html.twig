{% block navBar %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
        <a class="navbar-brand" href="{{ path('admin_index') }}">Administration</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_index') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_club_index') }}">Clubs</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_discipline_index') }}">Disciplines</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_equipment_index') }}">Equipements</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_brand_index') }}">Marques</a>
                </li>
            </ul>
        </div>
    </nav>
{% endblock %}