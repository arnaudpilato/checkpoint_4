{% extends 'base.html.twig' %}

{% block title %}Brand index{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center my-5">Marques</h1>
        <div class="row mt-5">
            <div class="col text-center">
                <p>Cette section concerne les différentes marques de monocycle</p>
            </div>
        </div>
        {% for brand in brands %}
            <div class="row mt-5 justify-content-around">
                <img class="img col-11 col-lg-5 p-0" src="{{ asset('uploads/brand/' ~ brand.picture) }}" alt="Image club">
                <div class="col-11 col-lg-5">
                    <h2 class="text-center mt-4">{{ brand.name }}</h2>
                    <p class="mt-3">{{ brand.description }}</p>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
